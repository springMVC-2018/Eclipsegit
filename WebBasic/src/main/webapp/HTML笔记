HTML 元素：是标签和属性还有文本内容的整体的统称。
html 里面只有 兩個子標籤“head”和“body” 。其中 head是用來做配置的。
div : 分區元素。
空格：多個空格只會顯示一個空格，如果需要多個空格需要使用代碼代替。
換行：一個換行或多個換行只會顯示一個空格。如果需要換行擇需要寫代碼代替。
6. 英雄联盟案例：
分为3个区域
宽度都是960。都水平居中
在每个div上加上这句话：
style="border:1px solid red;width:960px;margin:0 auto;"
"border:1px  ：边框   。solid red;  ：边框颜色。
属性居中：align="center"
7. 补充：
 创建web项目
 必须切换到javaEE视图（点击Eclipse右上角JavaEE）
 选择项目下的Deployment，右键点击Generate.....
 
 想要 p 中间的元素文本居中，得用以下方法：
 <p align="center">dsfadsfdsfsd</p> 
 
 特殊分区-也叫行分组，就是表格分区：
 表格可以划分为3个部分：表头、表主体和表尾。
 表头行分组：<thead></thead>
 表主体行分组：<tbody></tbody>
 表尾行分组：<tfoot></tfoot>
 
CSS :
内联方式：
  样式定义在单个的HTML元素中，也就是标签中, 缺点是不好换行，只对单个元素(标签)有用，其他元素不能复用。
内部样式表：
样式定义在HTML页的元素中：整个网页所有的元素都可以复用。
外部样式表：
将样式定义在一个外部的CSS文件中(.CSS文件)，由HTML页面引用样式表文件。

选择器：
元素选择器：
通过元素名来选择css作用的目标。如果页面中有多个相同的元素需要重用同样的效果，建议使用元素选择器。

类选择器：
类选择器允许以一种独立于文档元素的方式来指定样式- 语法为： .className{color:red;}
所有能够附带class属性的元素都可以使用此样式声明- 将元素的class属性的值设置为样式类名。
如：demo4里面的。

ID 选择器：id选择器 以 # 号开头。
id 选择器以一种独立于文档元素的方式来指定样式，他仅作用于 id 属性的值
语法为：#id值{}

派生选择器：
派生选择器用来选择子元素，分为2种：
 - 后代选择器
    选择某元素的所有后代（子孙）元素
 - 子元素选择器
    选择某元素的所有子元素。

伪类选择器：
伪类用于设置同一个元素在不同状态下的样式。
常用伪类：
- ： link
       向未被访问的超链接添加样式
-： visited
      向已被访问的超链接添加样式
-： active
     向被激活的元素添加样式
-： hover
     当鼠标悬停至元素上方时，向该元素添加样式。
-： focus
     当元素获取焦点时，向该元素添加样式。     





 